
  # Home Page Structure

  This is a code bundle for Home Page Structure. The original project is available at https://www.figma.com/design/R2CaRfLL70Q5RDzYQ8dlNS/Home-Page-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  